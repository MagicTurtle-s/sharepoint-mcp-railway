[phases.setup]
nixPkgs = ["python310", "pip"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install -r requirements.txt"
]

[start]
cmd = "python -m uvicorn mcp_sharepoint.server_http:app --host 0.0.0.0 --port $PORT"
